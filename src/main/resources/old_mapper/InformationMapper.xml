<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.example.test.old.old_dao.IInformationDao">
    <resultMap id="BaseResultMap" type="com.example.test.old.entity.Information">
    </resultMap>
    <select id="get" resultMap="BaseResultMap" parameterType="com.example.test.old.entity.Information">
        SELECT *
        from information
        where username = #{username}
    </select>
    <insert id="insert" parameterType="com.example.test.old.entity.Information">
        insert into information(username,clientName, birth, height1, weight1, date, num, age, energy, aveenergy, enweight,
                                lenweight, diseases,ffm,protein,muscle,ucre)
        values (#{username},#{clientName}, #{birth}, #{height1}, #{weight1}, #{date}, #{num}, #{age}, #{energy},
                #{aveenergy}, #{enweight}, #{lenweight}, #{diseases},#{ffm},#{protein},#{muscle},#{ucre})
    </insert>
    <update id="update" parameterType="com.example.test.old.entity.Information">
        update information
        set birth     = #{birth},
            clientName = #{clientName},
            height1   = #{height1},
            weight1   = #{weight1},
            date      = #{date},
            num       = #{num},
            age       = #{age},
            energy    = #{energy},
            aveenergy = #{aveenergy},
            enweight  = #{enweight},
            lenweight = #{lenweight},
            ffm = #{ffm},
            protein = #{protein},
            muscle = #{muscle},
            ucre = #{ucre}
        where username = #{username}
    </update>
    <update id="updatedis" parameterType="com.example.test.old.entity.Information">
        update information
        set diseases=#{diseases}
        where username = #{username}
    </update>
    <select id="getByDoctor"  resultMap="BaseResultMap" parameterType="com.example.test.old.entity.Information">
        select * from information
        where username in (select patient_username from doctor_patient where doctor_username=#{doctorUsername})
    </select>
</mapper>