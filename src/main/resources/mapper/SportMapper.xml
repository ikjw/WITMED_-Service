<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.example.test.dao.ISportDao">
<resultMap id="BaseResultMap" type="com.example.test.entity.Sport">
</resultMap>
<insert id="add"  parameterType="com.example.test.entity.Sport">
    insert into sport(user, id, cal, cdate, time_slot) values (#{user},#{id},#{cal},#{cdate},#{time_slot})
</insert>
<select id="day" resultMap="BaseResultMap" parameterType="com.example.test.entity.Sport">
    select id,cal,cdate,time_slot  from sport where user=#{user} and cdate like CONCAT('%',#{cdate},'%')
</select>
</mapper>